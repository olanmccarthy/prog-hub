# Docker build configuration
# This file configures Docker Compose behavior and can be safely committed

# Disable BuildKit for Amazon Linux 2 compatibility
# BuildKit requires newer kernel features that may not be available on all systems
DOCKER_BUILDKIT=0
COMPOSE_DOCKER_CLI_BUILD=0
