# Node Environment
NODE_ENV=production
COMPOSE_PROJECT_NAME=prog-hub-staging

# Database Configuration (MySQL)
DB_HOST=db
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=REPLACE_WITH_SECRET
DB_NAME=appdb_staging

# MySQL Root Password (for Docker container)
MYSQL_ROOT_PASSWORD=REPLACE_WITH_SECRET

# Next.js Configuration
DATABASE_URL=mysql://appuser:REPLACE_WITH_SECRET@db:3306/appdb_staging
NEXT_PUBLIC_API_URL=https://staging.olanfans.com

# Discord Bot Configuration (DISABLED for staging)
DISCORD_ENABLED=false
