export default function StandingsPage() {
  return (
    <div>
      <h1>Standings</h1>
      <p>Current standings for the active session.</p>
      {/* TODO: Display standings with custom algorithm calculation */}
    </div>
  );
}
